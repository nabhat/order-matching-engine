#pragma once

// Root include for Order Matching Engine (OME)
//
// All public interfaces are declared under the `ome` namespace.
// Individual components will be split into separate headers as the design evolves.

namespace ome {
    // forward declarations will live here
}
